<% @page_title = 'Create User' %>

<span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> <%= link_to("Back to List", {:action => 'index'}, :class => '') %>

<div class="user new">
  <h2>Create Agent</h2>

  <%= form_for(:user, :url => {:action => 'create'}) do |f| %>

    <%= render(:partial => 'form', :locals => {:f => f} ) %>

  
      <%= submit_tag('Create User') %>


  <% end %>
</div>
